# Changes — 2026-03-20

## Session 1

- `v5.0.3` — Created `readme/changelog/` directory and daily changelog process
- `v5.0.3` — Moved app version to `package.json` as single source of truth (was hardcoded in Footer.vue)
- `v5.0.3` — Added `__APP_VERSION__` define in `vite.config.js`, Footer.vue now reads version dynamically
- `v5.0.3` — Updated CLAUDE.md with versioning and change cataloguing instructions
- `v5.0.4` — Added `Twitter::sendThread()` method for posting reply-chain threads
- `v5.0.4` — Created `twitter:changelog` command — reads daily summary, posts numbered 🧵 thread
- `v5.0.4` — Scheduled `twitter:changelog` daily at 00:00 in Kernel.php
- `v5.0.5` — Fixed `sendThread()` to return sent/total counts, command now reports partial thread failures
- `v5.0.5` — Fixed last-tweet length calculation to reserve space for both footer and thread label
- `v5.0.5` — Fixed `twitter:changelog` signature docstring (defaults to yesterday, not today)
