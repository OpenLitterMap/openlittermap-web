# 2026-03-21

## Session 1
- (v5.0.6) Fix TrustProxies: set `$proxies = '*'` to resolve "too many requests" on login behind reverse proxy — all users were sharing one rate-limit bucket because Laravel saw the proxy IP instead of real client IPs
- (v5.0.7) Add BOOP shortcut to CLAUDE.md — bumps version, updates changelog, readme docs, and skills in one command
- (v5.0.8) Rename `readme/summary/` to `readme/changelog/`, update all references in CLAUDE.md, ChangelogTweet command, and tests
- (v5.0.9) Fix 20s profile load: pipeline 365 individual `Redis::getBit()` calls into single round-trip in `calculateStreak()`. Add lightweight `/api/user/profile/refresh` endpoint so `REFRESH_USER()` doesn't duplicate the full profile request
- (v5.0.10) Settings tab loads instantly: skip `FETCH_PROFILE()` when landing on settings, read all fields from `userStore.user` (populated by lightweight `/refresh`). Full profile only fetched for dashboard/photos tabs. Added all settings fields to refresh endpoint
- (v5.0.11) Tag search now uses i18n translated names — search matches and displays translated labels (e.g. "coke" shows "Coca-Cola"), searchable by both raw key and translation. Applies to objects, brands, materials, and categories across all locales
- (v5.0.12) Move `twitter:changelog` schedule from 00:00 to 07:00. Add React Native repo commit extraction — fetches daily commits from `openlittermap/v7` branch via GitHub API, merges with web changelog, labels entries `[Web]`/`[Mobile]`
