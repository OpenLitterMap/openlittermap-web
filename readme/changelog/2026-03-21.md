# 2026-03-21

## Session 1
- (v5.0.6) Fix TrustProxies: set `$proxies = '*'` to resolve "too many requests" on login behind reverse proxy — all users were sharing one rate-limit bucket because Laravel saw the proxy IP instead of real client IPs
- (v5.0.7) Add BOOP shortcut to CLAUDE.md — bumps version, updates changelog, readme docs, and skills in one command
- (v5.0.8) Rename `readme/summary/` to `readme/changelog/`, update all references in CLAUDE.md, ChangelogTweet command, and tests
