# 2026-03-22

- **v5.0.13** — Fix Spatie model_type namespace mismatch (`App\Models\User\User` → `App\Models\Users\User`) in `model_has_roles` and `model_has_permissions` tables; add AdminRoleTest
- **v5.0.14** — Fix scheduler: `withoutOverlapping()` on clustering:process-dirty, Twitter credentials moved to config/services.php (no more raw `env()`), production guards on all Twitter commands, TWITTER_* keys added to .env.example
- **v5.0.15** — Fix `cleanChange()` regex: add `/u` flag for UTF-8 em-dash matching in changelog tweet thread builder (16/16 Twitter tests passing)
- **v5.0.16** — Add `readme/Twitter.md` — automated Twitter commands documentation (schedule, data sources, tweet format, Browsershot, helper methods)
- **v5.0.17** — Gamified daily report: 2-tweet scoreboard thread with streak counter, season progress, milestone tracker, country podium with lead streaks, new cities, mission line (41/41 Twitter tests passing)
- **v5.0.18** — Rewrite changelog tweet: overview tweet + grouped [Web]/[Mobile] sections, remove GitHub API dependency, parse prefixes from changelog file only (44/44 Twitter tests passing)
- **v5.0.19** — Code review fixes: tweet 280-char truncation guard, partial thread failure detection, oversized changelog line truncation, `getAllTimeMetrics` bucket_date filter, `getNewCities` ORDER BY, `formatMilestone` 1M labels, remove dead data vars, batch lead-streak query (48/48 Twitter tests)
- **v5.0.20** — Rename `readme/OLMbot.md` → `readme/Twitter.md`, update CLAUDE.md reference and docs with v5.0.19 review fixes
