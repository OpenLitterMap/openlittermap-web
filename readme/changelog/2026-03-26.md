# 2026-03-26

## v5.2.4
- Fix: Map drift on refresh — `initializeMap` now uses URL lat/lon/zoom for initial position regardless of `load` param, eliminating 5-second flyTo animation from [0,0]

## v5.2.3
- Fix: Cluster markers simplified to single circle with box-shadow (removed two-ring design that looked off)
- Fix: Popup image onerror infinite loop — empty `src=""` triggered onerror, non-existent `error.png` caused endless 200 HTML responses from SPA catch-all
- Fix: Signed URL controller returns production S3 URLs directly in local dev instead of generating broken MinIO signed URLs
- Fix: Reset `margin` on `.marker-cluster div` to prevent MarkerCluster.Default.css from shifting cluster icons
