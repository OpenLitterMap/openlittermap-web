# 2026-03-27

## v5.2.5
- fix: 419 "Page Expired" on photo upload — refresh XSRF-TOKEN cookie after login, use custom FilePond process function to read token at request time instead of stale meta tag
