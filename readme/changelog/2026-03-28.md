# Changes

- **`v5.3.0`** — Twitter changelog bot now fetches mobile entries from react-native repo (`openlittermap/v7` branch) with graceful fallback
- **`v5.4.0`** — Web onboarding flow: Welcome → Upload → Guided Tag → Celebration. New users redirected to 3-step onboarding after signup/login. Quick-select chips for 6 common tags, StepIndicator, skip option. Backend: `onboarding_completed_at` column + set on first tag + skip endpoint
