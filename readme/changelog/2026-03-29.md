# 2026-03-29

## v5.4.1
- fix: LoginController now uses buildFullProfileData() instead of raw model (prevents leaking stripe_id, token, etc.)
- fix: Login response now includes XP and level data (consistent with register response)
- fix: Added onboardingNotCompleted guard — completed users can no longer re-enter /onboarding routes
- fix: Upload failure recovery UI — "Try again" button when all files fail during onboarding
- fix: Skip onboarding now shows toast error on failure instead of silent catch
- fix: Celebration page awaits REFRESH_USER to avoid flash-of-zero XP
- fix: Client-side username validation now matches backend regex (letters, numbers, hyphens, underscores)
- fix: reCAPTCHA conditionally enabled when VITE_RECAPTCHA_SITE_KEY env var is set
- perf: Tags store preloaded during onboarding upload (fire-and-forget)
- perf: Tags and photos load in parallel on tag page (non-edit mode)
- test: Added test_login_returns_onboarding_completed_at and test_login_does_not_leak_sensitive_fields
- fix: CHECK_AUTH catch block now resets store state (prevents stale auth after network errors)
- fix: Tag submission now checks UPLOAD_TAGS response.data.success before clearing tags
- fix: Skip onboarding sets onboarding_completed_at optimistically (prevents redirect loop if REFRESH_USER fails)
- fix: Double-submit guard on submitTags (early return if already submitting)
- fix: Nav hides Upload/Tags/Profile/Teams/Settings during onboarding (cleaner focused flow)
- perf: Photos preloaded after onboarding upload before navigating to tag page
- feat: Onboarding welcome page — replaced misleading "GPS captured automatically" with tabbed iPhone/Android geotagging setup instructions
- feat: Upload page shows GPS setup instructions when photos are rejected for missing location data
- feat: Celebration screen shows photo geolink with copy-to-clipboard button and "View on the map" CTA using GPS coordinates (`/global?lat=&lon=&zoom=17.89&load=true&open=true`)

## v5.4.2
- feat: GpsInstructions component — reusable tabbed iPhone/Android GPS enablement card
- feat: Onboarding welcome shows GPS setup instructions before first upload
- feat: Upload page shows GPS instructions when photo rejected for missing location data
- feat: Celebration geolink uses GPS coordinates (`lat`/`lon`) instead of photo ID
- docs: Created readme/Onboarding.md — full system documentation
- fix: Android GPS instructions now start with system Location toggle (most commonly missed step)
- feat: Android upload tip — use Browse view instead of Photos/Albums picker to preserve GPS metadata
- feat: iPhone step 5 — recommend Settings → Camera → Formats → Most Compatible (JPG over HEIC)
- i18n: All GpsInstructions strings use $t() with onboarding_gps_* keys in en.json

## v5.4.3
- fix: TagCard now uses translated brand names ("Coca-Cola" not "Coke", "7-Eleven" not "Seven Eleven")
- fix: TagCard material names also use i18n translations
- feat: Celebration screen — "See your upload on the global map" CTA, sharing copy, council tip
- feat: GPS privacy note — reminds users to turn Location Services off if they don't want other photos geotagged
- refactor: GpsInstructions i18n keys changed from onboarding_gps_* to full English string literals (matches codebase convention)

## v5.4.4
- fix: GPS instructions use → arrows and bold menu names instead of commas
- fix: Onboarding upload allows multiple photos (only first needs tagging)
- fix: Brand search includes both key and translated name in search index
- fix: Geolink now includes photo ID (`&photo={id}`) to load specific photo on map
- fix: Map popup shows "Awaiting verification" instead of infinite spinner for unverified photos

## v5.4.5
- fix: Removed onboarding guard from admin routes — admins no longer locked out of /admin/* during onboarding
- docs: Updated Onboarding.md (admin routes, geolink photo ID, multi-upload, sharing copy, verification note)
