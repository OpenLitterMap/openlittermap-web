# 2026-04-11

- 5.8.5 — Dependency security maintenance: `npm audit fix` resolved 7 advisories in transitive dev/build deps (axios 1.13.6→1.15.0 SSRF + cloud-metadata header injection, vite path traversal + dev-server WS arbitrary file read, lodash `_.template` code injection + `_.unset/omit` prototype pollution, picomatch POSIX method injection + extglob ReDoS, flatted prototype pollution + unbounded-recursion DoS, defu `__proto__` prototype pollution, immutable prototype pollution, brace-expansion zero-step hang). `composer update` pulled Laravel Boost to v2.4.3. No runtime code changes.
