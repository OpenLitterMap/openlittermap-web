# 2026-04-12

## v5.8.6 — Bug fixes: null state_id, null city, upload error logging
- Fix TypeError in TagsVerifiedByAdmin when photo has null state_id (nullable geocoding) 
- Fix null city crash in UploadPhotoController (wasRecentlyCreated on null)
- Add error logging to upload pipeline: validation 422s, geocoding failures, unexpected exceptions
- Catch GeocodingException in upload controller and return structured 422 instead of 500
- Add test: tagging photo with null state_id fires event without error
- Add test: upload succeeds when geocoding returns no state or city

## v5.8.7 — Fix horizontal scrollbar on LiveEvents
- Fix horizontal scrollbar on map LiveEvents panel caused by slide-in animation overflow

## v5.9.0 — Team data download parity with user export
- Add rich filter bar to team Photos tab: status, tagged/untagged, picked up, photo ID, tag search, custom tag, member, date range, per page
- Move Export CSV button from hub header into Photos tab filter bar (filters apply to export)
- Open team data export to all team members (was leader/school_manager only)
- Add backend filter support to TeamPhotosController::index() and CreateCSVExport
- Add TeamPhotosFilterTest (9 tests) and update DownloadTeamDataTest (member can now export)
- Dark glass theme for team photo grid and filter bar
