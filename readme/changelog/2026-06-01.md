# 2026-06-01

## Session: Sentry error reporting

- Wired Sentry into `app/Exceptions/Handler.php` via `register()` + `Integration::captureUnhandledException()` (Laravel 10 file structure — not `bootstrap/app.php`). DSN gated to `APP_ENV=production` in `config/sentry.php`. Investigating prod-only "Server Error" on user registration (user row + welcome email succeed, then 500 in the synchronous tail of `RegisterController::register()`).
