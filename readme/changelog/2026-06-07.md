# 2026-06-07

## Session: Idempotent upload + idempotent tagging (mobile P1/P2 fix)

- Upload (`POST /api/v3/upload`) is now idempotent: a duplicate (`user_id + datetime`) returns the existing `photo_id` with `{ success, photo_id, already_uploaded: true, tagged }` instead of a 422. Dedup moved from `UploadPhotoRequest::after()` into `UploadPhotoController` as a pure, side-effect-free lookup (no second Photo/S3/XP). Fixes the mobile "photo id must be an integer" loop (P1) retroactively for old app versions. (v5.11.5)
- `POST /api/v3/tags` gains an idempotent already-tagged guard: if the photo has a `summary`, returns `{ success, already_tagged: true, photoTags }` without appending — closes the retry double-count (P2) for ordinary `verified=0` users. (v5.11.5)
- `PUT /api/v3/tags` now stamps `onboarding_completed_at` on first non-empty tag (parity with POST), so the mobile auto-upload flow can tag exclusively via PUT; confirmed PUT-first-time == POST-first-time for verified/XP/metrics. (v5.11.5)
- Tests: rewrote duplicate-upload tests to the idempotent contract; added pure-lookup/no-XP, `tagged` flag, POST-guard no-op, and PUT-first-time/onboarding-parity tests (`UploadPhotoTest`, `AddNewTagsToPhotosTest`, `ReplacePhotoTagsTest`). 97 tests passing across touched suites. (v5.11.5)
- Consumer audit of the removed `UploadPhotoRequest` dedup: only the web SPA uploader (`Upload.vue`) relied on the 422 — it now gets the idempotent 200. Duplicate response carries `xp_awarded: 0`, and `Upload.vue` session-XP tally switched from `|| 5` to `?? 5` so duplicates (and deferred school uploads) count 0, not a phantom +5. (v5.11.5)
- Docs: `readme/API.md` and `readme/Upload.md` updated with the idempotent upload + tagging contracts. (v5.11.5)

## Session: Custom tag sanitization (500 fix)

- `AddTagsToPhotoAction::attachCustomTags()` no longer throws `\Exception('Invalid custom tag.')` on punctuation. The allowlist regex `/^[\w\s:-]+$/` rejected legit brand/product chars (e.g. `&` in "Black & Mild"), and since the throw lived inside `run()`'s `DB::transaction`, one cosmetically-bad custom tag 500'd the request AND rolled back the user's valid object tags. Replaced with sanitize-and-accept: `mb_substr(trim(strip_tags($key)), 0, 255)` (caps to `custom_tags_new.key` varchar(255)); empty-after-sanitize tags are skipped (`continue`), never thrown. Applies to both standalone (`createExtraTagOnly`) and object-attached (`createTagFromClo`) custom tags. `bn:`→brand resolution intentionally left for the separate "brands later" ticket — `bn:` tags store as literal custom strings. (v5.11.6)
- Tests: added 4 to `AddNewTagsToPhotosTest` — `&` saves, `. ' /` save, object+bad-custom both persist (no rollback), empty custom skipped not thrown. (v5.11.6)
- Output-path security review (custom tags are now free text): stored XSS confirmed safe across all render paths — map popups escape via `htmlSanitizer.escapeHtml` (textContent→innerHTML), Vue uses `{{ }}`, the one `v-html` (tag-search highlight) pre-escapes, API returns JSON, and custom tags never reach the Twitter impact reports. (v5.11.6)
- CSV export: decided to keep cell values **byte-exact** (no formula-injection prefix). PhpSpreadsheet quoting already prevents delimiter/quote/newline breakout; a `=+-@` prefix would corrupt programmatically-consumed research data (e.g. negative longitudes) for a warning-gated spreadsheet-only risk. Documented the verbatim caveat in `readme/ExportData.md` (treat `custom_tag_*`/`model`/`display_name` as untrusted when opening exports in a spreadsheet app) and logged a backlog ticket for a string-typed `.xlsx` export as the proper spreadsheet-safe path. (v5.11.6)
- Docs sync: deleted the working `readme/upload-spec.md`; corrected `readme/API.md` `custom_tags` rule (free text, 255-cap, not "max 100"); completed `readme/Upload.md` (POST already-tagged guard + PUT onboarding parity); added the idempotent duplicate-upload response to `readme/Mobile.md`; updated `readme/Onboarding.md` (PUT `update()` is now a third onboarding-completion trigger). Skills: `tagging-system` (custom-tag sanitization invariant). (v5.11.6)

## Session: HEIC upload fix

- Fixed HEIC photo upload (two bugs, both confirmed against production). **Bug 1:** `MakeImageAction` shelled out to `magick convert`, but the server has ImageMagick 6 (`convert`, no `magick`) — every HEIC conversion failed. Hard-switched to IM6 `convert input output`. **Bug 2:** `UploadPhotoRequest`'s `image` rule (excludes HEIC) and `dimensions` rule (`getimagesize()` returns false for HEIC) rejected genuine HEIC before it reached the converter. `rules()` now drops `image`+`dimensions` for HEIC (detected via the now-public `MakeImageAction::isHeic()` — magic-byte aware, catches iOS HEIC sent as `.jpg`), keeping `mimes`+`max`. Why "some users": only those sending genuine HEIC bytes failed; iOS auto-transcoded-to-JPEG uploads always worked. (v5.11.7)
- Test: added `HeicUploadTest::test_genuine_heic_uploads_successfully` — posts a real committed HEIC fixture (`storage/framework/testing/sample.heic`) to `/api/v3/upload` (mobile mode) and asserts success, with `MakeImageAction` faked (no `convert` binary locally/CI; Bug 1 verified on production). The prior HeicUploadTest only reflection-tested `isHeic()`, never the endpoint. (v5.11.7)
- Follow-up ticket (not implemented): web-mode HEIC (no explicit lat/lon/date) still hits `after()`'s `exif_read_data`/GPS checks, unreliable on HEIC. Mobile path fully fixed. (v5.11.7)
- Docs: `readme/Upload.md` (new HEIC handling section + fixed stale mobile duplicate-422 line), `readme/API.md` (`photo` rule note), `readme/Mobile.md` (HEIC supported). Skills: `photo-pipeline` (HEIC validation-skip + `convert`-not-`magick` invariant + Common Mistake). (v5.11.7)

## Session: Map popup picked_up fix

- Fixed the map popup "picked up" status not reflecting edits. Root cause: a read/write source mismatch — the popup's `picked_up` was derived from the deprecated photo-level `photos.remaining` (frozen at upload), while editing picked-up writes the per-tag `photo_tags.picked_up`. `PointsController::formatFeatures()` now reads `picked_up` from the first tag via the already-loaded summary (`data_get($photo->summary, 'tags.0.picked_up')`); untagged → `null` (popup hides the pill). Removed `photos.remaining` from the points query `select`. (v5.11.8)
- Tests (`PointsTest`): updated the two tests that encoded the old `remaining` behavior to per-tag summaries; added `it_uses_first_tag_picked_up_not_photo_remaining` (regression: `remaining=true` but tag `picked_up=true` → API returns true). Points suites: 69 passing. (v5.11.8)
- Follow-up (not done): the same `$photo->picked_up`-from-`remaining` pattern feeds the shared popup from `MapController:65`, `DisplayTagsOnMapController:82`, and `TeamsClusterController:181` (team maps / tags-on-map). Apply the same one-line derivation there to fully retire `photos.remaining` from the read path. (v5.11.8)
- `readme/API.md` `GET /api/points` section documents the per-tag `picked_up` derivation. (v5.11.8)

## Session: Fully retire photos.remaining from picked-up reads

- Centralized the fix in `Photo::getPickedUpAttribute()` — it now derives picked-up from the first tag (`data_get($this->summary, 'tags.0.picked_up')`, `?bool`, null for untagged) instead of `!$this->remaining`. Because `picked_up` is in `$appends`, this fixes **every** reader at once — map popups (`MapController`, `DisplayTagsOnMapController`, `TeamsClusterController`), public profile, uploads list, and team photo views — so `photos.remaining` is no longer read for picked-up anywhere. Reverted the inline `PointsController` derivation back to the accessor (DRY). (v5.11.9)
- `photos.remaining` now only persists as a write at upload (a user default) and as a deprecated legacy response field; it and `picked_up` can diverge for edited photos (expected — clients must use `picked_up`). `TagSummaryService` (dead, read `!remaining`) confirmed unused. (v5.11.9)
- Tests: updated `PhotoTest::test_picked_up_is_derived_from_first_tag_not_remaining` to the new behavior. Full suite green (1266 passing, 1 pre-existing S3/MinIO skip). (v5.11.9)
- Docs: `photo-pipeline` skill invariant #9 rewritten; `readme/API.md` points note centralized + removed the stale `POST /api/profile/photos/remaining/{id}` endpoint (not registered). (v5.11.9)

## Session: Remove deprecated `remaining` from API responses

- Completed the retirement: the deprecated photo-level `remaining` field is no longer returned in any API response. Added `remaining` to `Photo::$hidden` (`['geom', 'remaining']`) so it's stripped from all serialization, and removed the explicit `'remaining' => $photo->remaining` lines from `MapController` and `UsersUploadsController` (×2). The column still exists and is still written at upload (a user default) and read by CSV export + the v4→v5 migration source — only the API response field is gone. Clients use `picked_up`. (v5.11.10)
- Tests: removed `remaining` from the `UsersUploadsFiltersTest` response-structure assertion and added an `assertArrayNotHasKey('remaining', ...)` guard. No frontend reads the field. (v5.11.10)
- Docs: `readme/API.md` (`picked_up` note) and `readme/Mobile.md` (`picked_up` section) updated — `remaining` removed from responses; `picked_up` is now `true`/`false`/`null` (null for untagged). (v5.11.10)

## Session: Update #28 (changelog page + email)

- Published Update #28 ("1st TidyTowns Webinar | 1 Million Hours | EU Presidency Citizen Science Deliverable", 7th June 2026): added `resources/js/views/General/Updates/update28.vue` and registered it in `Changelog.vue` (visible at `/changelog`). Content covers the TidyTowns webinar + endorsements (Meath County Council, Dept of Rural and Community Development and the Gaeltacht), the 1 Million Hours challenge with 30-day growth stats (309 / 4,398 / 5,960), the EU Presidency Founding Members ask, the upload/tagging fixes shipped this branch, and The Democracy Gap dossier. (v5.11.11)
- Email: added `resources/views/emails/update28.blade.php` (same content, table-based HTML email layout) and repointed `App\Mail\EmailUpdate` (used by `olm:send-email-to-subscribed` → `DispatchEmail`) to subject "Update 28 …" and view `emails.update28`. Rendered/verified (unsubscribe token resolves). (v5.11.11)
