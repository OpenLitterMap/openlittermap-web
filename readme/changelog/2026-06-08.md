# 2026-06-08

## Session: readme/ audit & aggressive simplification (v5.12.2)

Audited all 33 `readme/` docs against the live code (parallel agents + direct verification of summary format, migration status, and LitterItem), then cut bloat, killed staleness, and consolidated duplicates. Net: **33 → 25 files, ~13,435 → ~9,740 lines (~27% reduction)**, with zero point-in-time artifacts and one source of truth per domain.

**Deleted — completed one-time artifacts / unbuilt spec (git preserves them):**
- `MobileApiReview.md`, `TaggingAuditReport.md`, `TagsCleanupPostMigration.md`, `Migration.md` (migration shipped — tooling pointer added to `PostMigrationCleanup.md`), `LitterItem.md` (specs a feature with no model/table/migration in code), and `changelog/production-orphan-fix-runbook.md`.

**Merged, then deleted the source:**
- `SchoolTeam.md` → `Teams.md` — folded the unique Logo Storage + Participant Sessions flow (~95% was already duplicated).
- `MobileAPI.md` → `API.md` — added a compact "Mobile Client Integration" section (API.md already documented the endpoints + tag-search index). Consolidated 7 scattered struck-through "REMOVED" stubs into one "Removed Endpoints" table in Reference.
- `Taggingarchitecturespec.md` → `Tags.md` (now the single 593-line tag doc). **Fixed the photo `summary` JSON to the authoritative flat-array format** (from `GeneratePhotoSummaryService`, replacing the stale nested-dict/`by_category` format); dropped DRAFT framing + the v4→v5 migration narrative + the "brands deferred" status.

**Trimmed — cut bloat, preserved reference content:**
- `Locations.md` 777→572 (removed the v4→v5 "Problems" narrative, verbose code dumps, demo migration SQL).
- `Admin.md` 491→400 (collapsed Phase 1/2/3 status narrative; moved post-migration "What to Delete" to a `PostMigrationCleanup.md` pointer).
- `Clustering.md` 441→422 (deduped global-vs-tile strategy; **fixed stale team dirty-tile content in 8 places** — `dirty_teams` was dropped; team clustering is on-demand).
- `ExportData.md` 447→445 (merged duplicated CSV-format prose; column definitions kept).
- `UserLifecycle.md` (removed the done-status table, a redundant verification table, and a dangling `FeatureLifecycleTest.md` reference).

**Staleness fixes outside readme/:**
- `CLAUDE.md` — "Current Branch: upgrade/tagging-2025" → "Current State (v5 — shipped to master)"; removed deleted files from the Domain Documentation index; fixed the stale `Mobile.md` description.
- `olm-architecture` skill — corrected the Photo Summary JSON example to the flat-array format.

**Dangling-link fixes:** removed dead `MigrationScript.md` and `Strategy.md` rows from `Metrics.md`'s Related Docs.
