# Changelog — 2026-06-14

## Session: Update #28 email/page polish (pre-send)

- Update #28 (`resources/js/views/General/Updates/update28.vue` + `resources/views/emails/update28.blade.php`) — edited in lockstep so the `/changelog` page and the member email stay in sync (no shared source: Vue is browser-rendered, the email is static inline-styled Blade — neither can render the other). (v5.12.5)
- Added the 1st TidyTowns webinar recording link (`youtube.com/watch?v=b8DwY3W9UEA`) to the webinar section.
- Renamed the HEIC "What's improved" bullet to "Apple's native .HEIC should be fixed for most users" and reworded the follow-on so it no longer repeats "Apple's HEIC".
- Added four "What's improved" bullets for fixes shipped after the update was first drafted: Android uploads + graceful "can't read this photo" message, login fix for shared-wifi schools/offices, CSV data export crash fixed, fresh level names ("Litter Picker").
- Added an App Store + Google Play download CTA (the email had dropped it vs Update #27; the page now matches Update #27's app-links pattern).
- Replaced the stale "Our next meeting is Thursday 11th June at 7pm" Founding Members line (date had passed) with a date-agnostic "email us and I'll send the time of the next call".
- Added the "Small action. Big impact." campaign poster to `public/assets/small-action-big-impact.png` (1024×1536) and embedded it in a new "👣 Take the First Step Challenge" section (page uses `/assets/...`, email uses the absolute `https://openlittermap.com/assets/...` URL). Challenge copy invites tagging a TidyTowns group + uploading 1 data point; adapted the source "drop feedback in the comments" line to an email-feedback CTA since the page/email has no comment thread.
- Fixed a Vite dev-server import error on the challenge image — referenced the `public/` asset via a runtime `:src="'/assets/...'"` binding instead of a static `src`, so Vite doesn't try to bundle a Laravel-public file. (v5.12.5)
- Reduced the challenge poster to 50% display size (page `.challenge-img` max-width 100%→50%; email image 540px→270px), aspect ratio preserved. (v5.12.5)
- Version → 5.12.5 (patch): Update #28 content polish + the new "Take the First Step Challenge" section. `package.json` + `package-lock.json` bumped together.
