# 2026-08-03

## Session — CSV export silently dropping migrated litter (v5.13.1)

- Investigated a user report of an undercounted plastic bag total for team 211; confirmed the v5 migration itself is exact (v4 `other.plastic_bags` reconciles 1:1 to v5 tag quantities, zero mismatched photos) — v5.13.1
- Found `CreateCSVExport` derived category/object columns from the `category_litter_object` pivot, so any tagged object without a pivot row got no column and its quantities vanished from every export — 189,518 tag rows / 277,169 items on production — v5.13.1
- Traced the shadow objects to `ClassifyTagsService::normalizeDeprecatedTag()` mapping 29 legacy keys to non-canonical object keys, which `classifyNewKey()`'s `firstOrCreate` fallback then created as real `litter_objects` rows — v5.13.1
- Fixed `CreateCSVExport` to build columns from `photo_tags` with a `LitterObject` key lookup, removing the pivot dependency so a tagged object always gets a column — v5.13.1
- Added regression test `test_tagged_object_without_a_category_pivot_still_gets_a_column`; verified no change to clean data (user 4051 `plastic_bag` = 1,576 before and after) — v5.13.1
- Added `readme/PostMigration-2026-08.md` documenting root cause, production impact, existing cleanup tooling and 11 known limitations — v5.13.1

## Session — external review follow-up (v5.13.1)

- Fixed a second silent-undercount vector found in external review: joined-format headers are synthesized as `{type}_{object}`, so a bare object keyed `beer_can` collided with canonical `can` + type `beer`, and associative CSV readers drop one of the two same-named columns — v5.13.1
- Second review round: the first collision fix renamed **both** sides, which would have removed the working `beer_can` header and broken every existing consumer; now only the legacy side is renamed (`legacy__beer_can__137`) and the canonical typed column keeps its header, with a final pass guaranteeing no renamed key can shadow an existing one — v5.13.1
- Reframed `readme/PostMigration-2026-08.md` to separate measured fact from inferred intent: the proven defect is the missing `category_litter_object` rows plus the exporter treating the pivot as authoritative, NOT that the migration chose "wrong" object keys — whether `plasticBags` was the intended v5 representation is recorded as an open product/taxonomy question — v5.13.1
- Documented that `olm:fix-orphaned-tags` performs **taxonomy consolidation** (changes object identity, category and type), not mechanical referential repair, and split the outstanding work into operation A (pivot repair — safe, fixes the export bug on its own, no ordering hazard since object identity never changes) and operation B (consolidation — needs product sign-off) — v5.13.1
- Corrected the mapping count from 75 to 74 (the earlier grep counted the `processMapping()` accessor) and dropped the unsupported "reviewed" claim, since only one mapping has been ID-validated — v5.13.1
- Added a joined-format regression test (the earlier test only covered split format); it landed as `test_joined_headings_disambiguate_legacy_and_canonical_collisions` after the round-3 rename — v5.13.1
- Simplified the active-object-id derivation to `pluck()->unique()` instead of a variadic `array_merge` spread — v5.13.1
- Corrected `readme/PostMigration-2026-08.md`: `olm:fix-orphaned-tags` and `olm:regenerate-summaries --orphan-fix` already exist with 75 reviewed mappings covering all 62 shadow objects, and `randomLitter` is already mapped to `other` — documented the real risks instead (unpaired summary regeneration, non-resuming partial runs, unvalidated hardcoded IDs, duplicate logical tags) — v5.13.1
- Decision: `ClassifyTagsService` and the v5 migration scripts are frozen as the source of truth for what the migration actually did and will NOT be corrected in place; any fix goes in a new dated post-migration command. Documented the operational guard instead (treat `AutoCreateBrandRelationships` as do-not-re-run, since it calls `normalizeDeprecatedTag()` then `firstOrCreate`s object + pivot) — v5.13.1
- Noted that the migration path could not have expressed several canonical targets anyway: `MigrationScript.php` has no `litter_object_type` handling and `UpdateTagsService`'s `'type'` is the `Dimension`, not `litter_object_type_id`, so `beerCan` → `can` + type `beer` was inexpressible; `FixOrphanedTags` is the corrective pass that applies types — v5.13.1

## Session — review round 3 (v5.13.1)

- Confirmed `plastic_bags => plasticBags` was the **intended** v5 migration representation; the documented defect is narrowed to the missing pivot row plus the exporter treating the pivot as authoritative. `FixOrphanedTags` is therefore a later taxonomy consolidation, not completion of the migration — v5.13.1
- Hardened the joined-header collision fix: non-colliding headers are now reserved before any legacy name is generated (a generated name can no longer displace a real column), ambiguous collision groups rename every member deterministically instead of crowning `$group[0]`, and column sort now tie-breaks on object/type id instead of inheriting database order — v5.13.1
- Found that pivot repair is **not** product-neutral: `GetTagsController::getAllTags()` filters on `whereHas('categories')`, so creating pivots would make all 62 legacy objects selectable in the tag picker. Documented as a required pre-check — v5.13.1
- Neutralised "shadow/wrong key" wording across the export comment, both regression tests and the incident doc — v5.13.1
- Added the full tag & key audit workstream (all litter_objects, categories, pivots, types, materials, custom tags, brands, TagsConfig) as a prerequisite to any taxonomy consolidation, plus a 10-step local rehearsal workflow against a production dump — v5.13.1

## Session — tag & key audit against production dump (v5.13.1)

- Imported production as `olm_postmig_2`; confirmed the local copy reproduces the defect exactly (189,518 pivotless rows / 277,169 items) — v5.13.1
- Audited all 198 `litter_objects` against `TagsConfig`: a clean split of 136 canonical (all with pivots) + 62 legacy (all pivotless, all carrying live tags), with zero unused rows available to delete — v5.13.1
- Identified **16 legacy keys with a canonical twin** (`plasticBags`/`plastic_bag`, `straws`/`straw`, `brokenglass`/`broken_glass`, `facemask`/`face_mask`, …) where the legacy key holds the historical bulk and the canonical twin only post-migration tagging — v5.13.1
- Flagged `paper`/`papers` as a duplicate-detection **false positive**: both canonical, different categories (other vs smoking = rolling papers), must stay separate — v5.13.1
- Found the proposed consolidation would collapse **36,391 items into generic `other`** objects (`randomLitter` 33,801, plus `magazine`, `books`, `lego`, `toothpick`, `hair_tie`, `ear_plugs` which have no canonical equivalent) — recommended adding ~6 objects to `TagsConfig` to preserve them — v5.13.1
- Documented the **10 category moves** the consolidation performs, flagging `facemask` sanitary→medical (7,910 items) and `lego` marine→other (likely wrong — lego is a known marine litter item) — v5.13.1
- Added `readme/TagAudit-2026-08.md` with the full 62-object review table, operation mix (36 merge / 19 merge+type / 10 category move / 0 unmapped) and six decisions requiring sign-off — v5.13.1

## Session — audit correction, pair-level (v5.13.1)

- **Corrected a methodological error**: the first audit classified *objects*, but the migration, pivot table, exporter and `FixOrphanedTags` all operate on **(category, object) pairs**. Verified 74 pivotless pairs across 71 objects — 62 legacy keys plus **9 canonical objects tagged in a category they have no pivot for** (6,932 rows / 10,812 items) that an object-level review misses entirely — v5.13.1
- Consequently: category moves are **17, not 10**, and generic-`other` mappings affect **39,123 items, not 36,391** (the extra 2,732 being canonical marine `bag` and `bottle`) — v5.13.1
- Simulated all 74 mappings read-only: **705 logical duplicate groups** (1,454 rows / 3,067 items), 597 colliding with an existing canonical tag, 317 carrying extra tags, and **280 whose colliding rows have DIFFERENT material/brand/custom-tag sets**. Cleanup must therefore **repoint rows and keep them physically separate**; row-level dedup is a separate later operation with a dimension-aware rule — v5.13.1
- Corrected the crisp size claim: `TagsConfig` defines `crisp_packet` with materials only (no `sizes`), and `photo_tags` has **no size or state column**, so size cannot be stored per observation — recommend keeping `crisp_small`/`crisp_large` distinct until a real size model exists — v5.13.1
- Flagged that retiring keys requires updating `app/Tags/BrandsConfig.php`, which still references `plasticBags` (12×), `beer_can` (13×), `crisp_small` (4×) and `crisp_large` (3×); `AutoCreateBrandRelationships` reads it and would recreate retired objects and pivots — v5.13.1
- Recommended rejecting the lossy marine mappings (`bag`, `bottle`, `straws`, `balloons` → `marine/other`; `lego` → `other/other`) in favour of adding the appropriate canonical pivots — v5.13.1
- Recorded that all 74 mappings are now **structurally** validated against `olm_postmig_2` (ids resolve, target CLOs match their pair, exact 1:1 pair coverage, no type overwrites) while the **target taxonomy remains unapproved** — v5.13.1

## Session — taxonomy inventory & decision manifests (v5.13.1)

- Generated four read-only deliverables in `readme/audit/` from `olm_postmig_2` with deterministic ordering and sha256 checksums; no data modified and no migration code touched — v5.13.1
- `LitterObjectInventory-2026-08.csv` (198 rows) — every litter object with measured evidence: pivot categories, per-category row/item/photo counts, pivotless pair counts, configured materials/types, and reference counts across `TagsConfig`, `BrandsConfig`, `ClassifyTagsService`, `FixOrphanedTags`, translations and the rest of `app/` — v5.13.1
- `LitterObjectDecisions-2026-08.csv` (198 rows) — proposed disposition per object (127 keep / 47 merge / 13 review-generic / 9 canonical-wrong-category / 2 retain), with information-loss, export impact, blockers and blank approval columns — v5.13.1
- `TagPairMigrationManifest-2026-08.csv` (74 rows) — every affected (category, object) pair with exact source/target category/object/type, operation flags, loss assessment and recommendation; totals reconcile to exactly 277,169 items, confirming complete coverage — v5.13.1
- `TagCleanupSummary-2026-08.md` — seven unresolved decisions and aggregate impact only — v5.13.1
- Duplicate detection proposes candidates but never auto-approves: 34 objects across 17 normalised-key groups, with `paper`/`papers` explicitly flagged `NORMALISED MATCH BUT DISJOINT CATEGORIES` — v5.13.1
- 5 pairs (3,596 items) marked REJECT — canonical `bag`, `bottle`, `balloon`, `straw` and `lego` should get marine pivots rather than collapsing to `marine/other` — v5.13.1
- 21 objects flagged with a `BrandsConfig` blocker, since retiring them without updating that config lets `AutoCreateBrandRelationships` recreate the object and pivot — v5.13.1


## Session — branch simplified to the minimum fix (v5.13.1)

- Reduced the branch to two things: the **export fix** (which alone resolves the reported bug) and a **queue-driven one-tag-at-a-time migration tool**. Everything speculative was removed or preserved elsewhere — v5.13.1
- Preserved the taxonomy audit work on branch `audit/2026/08/tag-taxonomy`, then removed from this branch: `olm:tag-review` + its test + queue, `LitterObjectDecisions` CSV, `TagAudit-2026-08.md`, and the unrelated `readme/LitterWeek.md` (swept in by a `git add .`) — v5.13.1
- Replaced `olm:repair-tag-pivot` with **`olm:migrate-tag`** — a single-transformation, queue-driven taxonomy migration. One entry, one run; there is no bulk apply — v5.13.1
- Migration is driven by `readme/audit/TagMigrationQueue-2026-08.csv`, one row per **(category, object) pair** with a 9-stage lifecycle (IDENTIFIED → … → COMPLETE). `--apply` is refused until the entry reaches `CODE_UPDATED`, so manual config/code work cannot be skipped. Status transitions must be sequential, require `--by`, and `*_VERIFIED` requires `--evidence` — v5.13.1
- The command repoints an **immutable pre-snapshotted set** of `photo_tags` to the target object + CLO, preserving category, type, quantity, extras and physical rows; regenerates summaries; re-runs `MetricsService` for processed photos so per-object Redis/`processed_tags` counts move from the old object id to the new one while totals and XP stay put; and repoints `user_quick_tags.clo_id` — v5.13.1
- The source `litter_objects` row is deliberately left as an unselectable tombstone; deleting retired objects is a later garbage-collection pass — v5.13.1
- **Dropped the write-path and tag-picker enforcement as unnecessary**: `olm:migrate-tag` never creates a pivot (it repoints to an existing canonical CLO and hard-fails otherwise), so a historical pair such as `other/plasticBags` never becomes writable. Guarding it added risk and broke 24 Quick Tags tests for a situation the design cannot produce — v5.13.1
- `tests/Feature/Migration/MigrateTagTest.php` (14 tests): apply gating by status, sequential transitions, author/evidence requirements, expectation mismatch, wrong target CLO, repoint-preserves-everything, tombstone survival, colliding rows never merged, summary points at the new object, XP does not move, rerun refuses, and that no bulk-apply flag exists — v5.13.1
