# 2026-08-08

## Session — tag retirement process design (v5.13.1)

- Defined "problematic tag" as *a tag we previously used and no longer want*, and applied it to the 74-pair manifest: it splits into **62 retirements** and **9 missing-pivot cases** that are not retirements at all (`marine/bag`, `other/dogshit`, `sanitary/gloves` and 6 more are tags we keep, just tagged in a category with no pivot) — v5.13.1
- Established that merge direction is **not derivable** from `TagsConfig` membership: `TagsConfig` is not consistently snake_case (`rollingPapers`, `vapePen`, `brokenGlass`, `pullRing`, `earSwabs` sit alongside `plastic_bag`). The list now carries an owner-set `desired_key` per row rather than inferring the survivor — v5.13.1
- Measured the 16 spelling-twin pairs: the camelCase/plural side holds **36,483 rows**, the snake_case/singular twin holds **791**. Retiring the low-volume side moves 46× less data and leaves `ClassifyTagsService:228` correct as written, removing the frozen-file override problem — v5.13.1
- **Decision: retire `plastic_bag` (92), keep `plasticBags` (149)** — inverts the direction the existing `TagMigrationQueue` row assumed, and with it all three of `olm:migrate-tag`'s data-asserted boundary guards — v5.13.1
- Found **128 `user_quick_tags` rows on CLO 111** — the Quick Tags migration deleted last session ("a pivotless source cannot hold a stored CLO reference") must return, because that reasoning only held in the old direction — v5.13.1
- Found the Redis verification gap: `RedisMetricsCollector` writes object counts to `:obj` hashes **and** `rank:objects` ZSETs at global/country/state/city, plus `obj:{id}` in every contributing user's `{u:ID}:tags` — the command verifies one global hash out of that — v5.13.1
- Confirmed `olm_postmig_2` is unmutated: 189,518 pivotless rows / 277,169 items, matching the documented incident baseline exactly — v5.13.1
- `readme/PostTagMigrationClean.md` — new design document covering the list, the per-tag A/B/C/D pipeline, the `retired_at`/`merged_into_id` schema change, the 12-surface verification table, and the rehearsal loop. For review; nothing implemented — v5.13.1

## Session — post-migration doc consolidation (v5.13.1)

- **`PostMigration-2026-08.md` reduced to a pure incident record** (27KB → 12KB). It was doing two jobs badly: documenting a defect *and* proposing a cleanup workflow that has since been superseded twice. The forward process now lives solely in `PostTagMigrationClean.md` — v5.13.1
- Fixed a **dangling reference**: the doc cited `readme/TagAudit-2026-08.md` twice as authoritative, but that file was deliberately moved to branch `audit/2026/08/tag-taxonomy` on 2026-08-03. Now points at `readme/audit/TagCleanupSummary-2026-08.md` and the inventory CSV, which is where the content actually is — v5.13.1
- Deleted as superseded: the all-at-once run sequence, the 10-step local rehearsal workflow, the operation A/B pivot-repair framing (it assumed the merge direction reversed by decision D-4), and limitations 3–6, which documented internals of `olm:fix-orphaned-tags` — a command that will never run. Its do-not-run warning is retained as a standalone section — v5.13.1
- Moved rather than dropped: the `AutoCreateBrandRelationships` do-not-re-run guard → `PostTagMigrationClean.md` §5, now also stating that the `BrandsConfig` repoint must land in the same commit; the unaudited read surfaces (map popups, profile breakdowns, mobile, achievements) → open question 5; the PHP-`sortBy`-vs-MySQL-collation column ordering caveat → `ExportData.md`, where it is an export behaviour — v5.13.1
- Retained as still-live outstanding items: duplicate export columns, no production repair run, and the historical undercount affecting researchers — the last explicitly marked unowned — v5.13.1

## Session — design review response (v5.13.1)

- **Added Step 0: close the picker before moving data.** `GetTagsController::getAllTags()` filters on `whereHas('categories')` only, so a retired key stays selectable until its pivot is deleted at the end of the run. Any tag created mid-run falls outside the immutable snapshot, survives retirement, and fails `--verify` with no remediation. `retired_at` is now set *first*, and the picker filters on it — this ordering is the strongest argument for the schema change, since the pivot can't be deleted early while `photo_tags` still references it — v5.13.1
- **Found a latent double-count**: `MigrateTag:468` gates the metrics re-run on `processed_at !== null` alone. `MetricsService::deletePhoto()` has already reversed metrics for a soft-deleted photo, so `processPhoto()` on one re-adds them, inflating XP and object counts and breaking absolute Redis reconciliation. Guard is now `processed_at IS NOT NULL AND deleted_at IS NULL`; rows are still repointed. Measured zero soft-deleted instances for entry 1, so it is latent rather than active — v5.13.1
- Added the location tag endpoints to the verification table — `/api/locations/{type}/{id}/tags/{top,summary,by-category,cleanup,trending}` all serve an `objects` dimension off the location-scoped Redis hashes and render keys to users; plus `/api/user/top-tags` — v5.13.1
- Documented that the snapshot and the "zero rows on retired id" assertion both span tags on soft-deleted photos (`photo_tags` rows survive a soft delete; query it directly with no join to `photos`) — v5.13.1
- Fixed a doc bug: retired-CLO deletion was listed as both A.6 and B.2. Single home in B.2, with the note that by then the picker already excludes the object via `retired_at`, so deleting the pivot removes it from the **export**, not the picker — v5.13.1
- Verified and rejected two review items: there is **no tag cache** in this repo (`GetTagsController` has no `Cache::` call; no `tags_cache*` key in `app/`, `routes/`, `config/` or `resources/js/` — both endpoints read live from MySQL), and **`resources/data/tag_usage_counts.json` / `tag_counts_public.json` do not exist** here. Noted the conditional requirement in case caching is added later — v5.13.1
- Recorded the recommendation on open question 2 (export headers): clean break per column plus a published `retired_key → desired_key` mapping page, rather than carrying dual columns for 16 twins — v5.13.1

## Session — snapshot harness (v5.13.1)

- `olm:tag-retirement-snapshot` — read-only before/after measurement harness for one retirement. `--entry`, `--label`, `--diff`, `--list`. Captures MySQL row/item/photo counts per object plus global totals, pivot and object state, summary references, Redis across every affected scope, quick tags, the tag-picker response and real export headings. No writes anywhere — v5.13.1
- **Determinism verified**: `--label=a` and `--label=b` back-to-back produce byte-identical artefacts (sha256 `04445e2d…`, 284,007 bytes). All maps recursively key-sorted; timestamp, database name and label live in a `.meta.json` sidecar that is never diffed — v5.13.1
- **Baseline guard** asserts 189,518 pivotless rows / 277,169 items before writing; mismatch aborts with no artefact, so a mutated restore can't produce an authoritative-looking capture — v5.13.1
- **XP weighting captured as a computed precondition, not an assumption** — the harness resolves `XpScore::getObjectXp()` for both keys and records equivalence. Entry 1 is 1 → 1 (invariant holds); the same check will correctly refuse to assert invariance for `bags_litter` (10) → `bagsLitter` (1) — v5.13.1
- **Redis is empty on this machine** (`dbsize` 0) — all 1,196 affected scopes and 450 user hashes captured as null. No Redis assertion in the design is exercisable until `olm:redis:rebuild` runs against `olm_postmig_2` — v5.13.1
- **Design doc correction found by measurement**: `plasticBags` already has an export column in both captured scopes, because the v5.13.1 fix derives columns from `photo_tags` rather than the pivot. §4 A.2's "needs a pivot to earn an export column" and §4 B.2's "deleting the pivot removes it from the export" are both wrong — the pivot governs tag-picker visibility only — v5.13.1

## Session — Redis rebuild blocker (v5.13.1)

- **`olm:redis:rebuild` cannot complete on this database.** It died after 15,727 of 536,947 photos with `TypeError: updateUserMetrics(): Argument #2 ($userId) must be of type int, null given`. **151 processed, live photos have `user_id = NULL`** and `photos.user_id` is nullable in schema — v5.13.1
- **The failure is fatal rather than logged** because `RedisMetricsCollector::processPhoto()` catches `\Exception` at line 81, and `TypeError` extends `Error`. The design doc's claim that "MetricsService logs Redis failures without raising, so this can drift silently" holds for exceptions but not for type errors, which kill the process instead — v5.13.1
- **This blocks entry 1 directly**: photos 181750, 181751 and 181753 are processed, live, null-user and carry entry-1 tags. `MigrateTag` catches `Throwable` per photo, so a run degrades to "3 photos failed, re-run to retry" — permanently, since the `TypeError` is deterministic — v5.13.1
- Redis left in a partial state (dbsize 8,308; `{g}:obj` holds 91 of the expected objects, object 149 reads 1,072 against a MySQL truth of 12,946). **Must be flushed and rebuilt after the fix** — any reconciliation against the current contents is meaningless — v5.13.1
- Corrected `readme/PostTagMigrationClean.md` §4 A.2 and B.2 per the export-column finding, and recorded the null-user defect as blocking open question 0. No `tags_cache_v7` references existed in the doc to remove — v5.13.1
- **Stopped before Step 2.** No migration, no `supportedScope()` rework, no `--apply` against any database — v5.13.1

## Session — metrics null-user + Throwable fixes (v5.13.1)

- **Fixed ownerless-photo handling across the whole metrics path**, not just the crash site. `photos.user_id` is nullable and 151 processed rows have no owner. Location and global metrics now still count that litter; every user-scoped write is skipped — v5.13.1
- **The crash was masking six silent corruptions.** `Redis::pipeline($callback)` is `tap($pipeline, $callback)->exec()`, so a throw inside the closure discards every queued command. The `TypeError` at `updateUserMetrics()` was therefore *preventing* the `(string)null → ""` writes queued before it at `RedisMetricsCollector` lines 40, 43, 46, 55, 65 and 69 — a phantom member in the contributor HLL, the contributor ZSET and **the global XP leaderboard**. Fixing only the type hint would have converted a loud crash into silent leaderboard pollution, so all six are guarded — v5.13.1
- **Found a second, independent `TypeError` site on the MySQL side**: `MetricsService::buildTimeSeriesRows()` passes `$photo->user_id` into `buildSingleRow(… int $userId = 0)`. The Redis rebuild never hit it because `RebuildRedisCommand` calls `RedisMetricsCollector::processPhoto()` directly, but the retirement path goes through `MetricsService` and would have. Ownerless photos now write the aggregate row only — correct anyway, since `metrics.user_id` is `NOT NULL` and part of the primary key — v5.13.1
- Guarded the three `User::where('id', $photo->user_id)->update(...)` XP sites. These were already silent no-ops (`id = NULL` matches nothing) but relied on an implicit SQL nuance rather than stating the intent — v5.13.1
- `catch (\Exception)` → `catch (\Throwable)` at `RedisMetricsCollector::processPhoto():81` and `getUserMetrics():218`. `MetricsService` has no catches at all, by design — it must not swallow MySQL failures — v5.13.1
- `tests/Unit/Redis/RedisMetricsCollectorNullUserTest.php` (7 tests): ownerless photo does not throw; global and location metrics still recorded; no user-scoped keys written; contributor HLL, contributor ZSET and XP leaderboard all untouched; delete path safe; owned photos unaffected; and an `Error` inside Redis processing is logged rather than propagated — v5.13.1
- Existing suites green: `tests/Unit/Redis` + `tests/Feature/Metrics` 42 passed (254 assertions); leaderboard + school metrics 22 passed (106 assertions) — v5.13.1
- Added absolute MySQL↔Redis reconciliation to `olm:tag-retirement-snapshot`, covering every location scope and every contributing user hash, recording only mismatches. It also reports `photo_tags` vs `processed_tags` drift, since Redis is replayed from the latter while the retirement operates on the former — v5.13.1
- **Redis rebuilt clean**: 536,947 photos replayed, uploads and XP reconcile exactly. Objects 92 and 149 reconcile with **0 mismatches across 1,236 scope checks and 460 user hashes**, source drift 0, capture deterministic (sha256 `266965c7…` ×4) — v5.13.1
- Noted but out of scope: the rebuild's own self-check reports **global litter MySQL 834,188 vs Redis 808,849**, a 25,339 gap that does not involve either entry-1 object — v5.13.1

## Session — retirement pipeline for entry 1 (v5.13.1)

- `litter_objects.retired_at` + `merged_into_id` migration. Retirement becomes an explicit fact rather than an accident of a missing pivot — which mattered because the pipeline now deliberately *creates* pivots — v5.13.1
- `GetTagsController::getAllTags()` filters through a new `LitterObject::active()` scope, so a retired object leaves the tag picker even while it still holds a pivot — v5.13.1
- `readme/audit/TagRetirements-2026-08.csv` — the single retirement list, entry 1 at `MAPPING_APPROVED`, direction per D-4, carrying both sides' measured counts and the XP weighting for each key — v5.13.1
- **`olm:migrate-tag` reworked for the D-4 direction.** `supportedScope()` inverted: it now refuses typed rows on the retired side and objects tagged across multiple categories, and **refuses any retirement whose two keys carry different XP weights** unless the list records `xp_equivalent=accepted` — the `bags_litter` (10) → `bagsLitter` (1) case cannot slip through silently — v5.13.1
- Apply now runs Step 0 first (`retired_at` + `merged_into_id`, closing the picker), then creates the surviving object's pivot, repoints rows idempotently, repoints quick tags off the retired pivot (dropping only rows that would collide with an existing preset), regenerates summaries, re-runs metrics **only for processed AND non-deleted photos**, and finally drops the now-dangling retired pivot — v5.13.1
- `--verify` added: ten assertions covering drained rows, pivot state, `retired_at`/`merged_into_id`, quick tags, picker inclusion/exclusion, summary references and absolute Redis reconciliation. `--advance=COMPLETE` runs it and refuses on failure — v5.13.1
- Resurrection guard in `AddTagsToPhotoAction::createTagFromClo()` — a stale CLO id can no longer write new tags onto a retired object (422). `AutoCreateBrandRelationships` now skips retired objects rather than handing them a fresh pivot — v5.13.1
- `MigrateTagTest` reworked to 28 tests for the inverted direction: apply gating, refusals (types, multi-category, XP-weight), pivot creation, picker closure and exclusion, tagging refusal, quick-tag repointing and non-interference, collision preservation, summary and XP invariance, verify pass plus three verify-failure cases, COMPLETE gating, and resume blocking — v5.13.1
- Dry run passes against real production data on `olm_postmig_2` (253 rows / 264 items / 253 photos, all matching). **No `--apply` has been run against any database** — v5.13.1
- Suites green: Redis + Metrics + Tags + Migration 238 passed (2,008 assertions); Photos + QuickTags + Leaderboard + Teams 268 passed (1,019 assertions) — v5.13.1

## Session — rehearsal harness + litter-gap ticket (v5.13.1)

- **Filed the litter gap as its own ticket** (`PostTagMigrationClean.md` §8a), unbundled from the retirement. Reframed after review: this is **rebuild fidelity**, not a claim about production Redis, which has been maintained incrementally by `MetricsService` all along and was never in the comparison. A freshly rebuilt Redis reconciles uploads and XP exactly but reads litter 808,849 against the `metrics` table's 834,188 (−25,339), with `photo_tags` summing to a third figure of 837,278 — v5.13.1
- That framing makes it more consequential, not less: `olm:redis:rebuild` is the documented remediation whenever Redis and MySQL disagree, so **rebuilding production Redis would silently move the global litter figure**. Marked do-not-run on production until understood. `--verify` is unaffected — its Redis assertion is scoped to the two retirement objects, which reconcile exactly — but its remediation hint now carries the same warning — v5.13.1
- `app/Console/Commands/tmp/v5/PostMigAug2026/rehearse-entry1.sh` — the rehearsal loop as one command. Each iteration restores from the `.sql` source of truth, **re-runs the migration and rebuilds Redis** (the dump predates `retired_at` and carries no Redis state, so skipping either fails run 1 confusingly), captures `before`, applies, verifies, captures `after`, diffs, and hashes all three artefacts. Ends by asserting the hashes are identical across iterations — v5.13.1
- The script refuses to run against a database whose name is not a rehearsal name, without a `DUMP` path, or with a dump file that does not exist — all three guards exercised — v5.13.1

## Session — entry 1 rehearsal PASSED (v5.13.1)

- **Three full iterations, byte-identical**: before `03051759`, after `e820e905`, diff `af9cfd29`. Verify 10/10 on every run — v5.13.1
- Globals unchanged: 837,278 items · 617,044 rows · 536,956 photos · 4,744,284 XP. Affected-photo XP unchanged at 166,857 — v5.13.1
- **Redis: 1,196 / 1,196 scopes and 450 / 450 user hashes** satisfy `retired = 0` and `desired = prior desired + prior retired`; post-apply reconciliation clean, 0 mismatches — v5.13.1
- Picker flipped (`plastic_bag` out, `plasticBags` in); team 211 export 193 → 192 columns with the retired column gone; quick tags 128 moved off CLO 111 onto the new pivot; desired object 12,946 → 13,210 items; summaries 253 → 0 referencing the retired object; pivotless rows 189,518 → 179,467 — v5.13.1
- Three rehearsal iterations were needed to expose two harness defects a single run passed clean over: the incident baseline guard fired on the retirement's own intended effect (it is a pre-state check, now asserted only for `before`, with the pivotless counts recorded in the payload instead), and `retired_at`'s value was being written into the diffed payload despite the file's own contract forbidding timestamps (now recorded as presence) — v5.13.1
- Entry 1 deliberately left at `MAPPING_APPROVED`. Advancing past `CODE_UPDATED` would assert the TagsConfig/BrandsConfig/locale work, which is step B and has not been done. Rehearsal evidence recorded in the list's `verification_evidence` instead — v5.13.1
- Measurement gap noted: the harness records export column **presence**, not cell values. The 13,210 figure is confirmed in `photo_tags`, not read out of a generated CSV — v5.13.1
