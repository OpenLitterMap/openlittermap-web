# 2026-08-09

## Session — branch review and reduction (`bugs/2026/08/plastic-bag-count`)

### Quality pass (`/simplify`)

- `AddTagsToPhotoAction::createTagFromClo()` now eager-loads `litterObject` on the CLO lookup instead of issuing a second `LitterObject` query per tag — removes one query per tag from every write path (web, mobile, admin approve, teacher approve) — v5.13.1
- Deleted `LitterObject::mergedInto()` — zero callers; `isRetired()` kept and now used at both guard sites instead of two hand-rolled `retired_at` predicates — v5.13.1
- `olm:migrate-tag` — dropped `baseline_rows` and `desired_clo_id` from the run state (written, validated on load, never read) and the `saveState` call that existed only to persist them — v5.13.1
- `olm:migrate-tag` — dropped `$photo->fresh()` before `MetricsService::processPhoto()`; the callee refetches under `lockForUpdate` and discards the model passed in, so that was ~10k redundant full-row reads on a 61-column table — v5.13.1
- `summariesReferencing()` — added a LIKE prefilter and switched `chunk()` (OFFSET-paginated, ~71M rows visited over 533k summaries) to `chunkById()`. Decode stays in PHP deliberately: `photos.summary` is TEXT, so a MySQL-side JSON predicate would make the gate error on a malformed row rather than degrade — v5.13.1
- Removed the `retired_at` index from the retirement migration — `litter_objects` has 198 rows, so it would never be chosen and costs a secondary index on every write — v5.13.1
- Merged a stray double docblock on `RedisMetricsCollector::processPhoto()` (PHP binds only the second; the first was an orphaned floating comment) — v5.13.1
- `MigrateTagTest` — `applyOk()` helper replaces 13 copies of the same two-line apply preamble; `insertQuickTag()` replaces two 10-line raw inserts; removed a redundant `RefreshDatabase` + Redis-flushing `setUp`/`tearDown` already provided by the base `TestCase` — v5.13.1

### Branch reduction (approved cut list)

- **Deleted `olm:tag-retirement-snapshot` (`TagRetirementSnapshot.php`, 965 lines)** and folded its absolute all-scope MySQL↔Redis reconciliation into `olm:migrate-tag --verify`. The rehearsal instrument and the production gate are now the same tool — previously `--verify` checked one global Redis key while the harness checked every location scope and user hash, so a retirement could pass the gate with its country/city/user hashes still crediting the retired object — v5.13.1
- `--verify` reduced from ten assertions to five, each independently failable. The five removed restated each other or self-checked work the command had just performed in the same run — v5.13.1
- The quick-tags assertion now checks **survival on the surviving pivot**, not absence on the retired one. `user_quick_tags.clo_id` cascades on delete and the retired pivot is dropped at the end of a run, so the old check passed whether the 128 saved presets were repointed or silently destroyed — v5.13.1
- Removed the `xp_equivalent=accepted` override. XP-weight divergence between the two keys is now an unconditional refusal in `supportedScope()` (decision D-6). Entry 1 is 1→1 so the mechanism was dead code; it returns when a non-equivalent entry is scheduled — v5.13.1
- Removed `--list`, `listQueue()` and `nextStepFor()`; compressed `advance()`'s four guard blocks into one `match` while keeping every rule (known status, `--by` attribution, sequential transitions, evidence on `*_VERIFIED`, verify-gates-COMPLETE) — v5.13.1
- `rehearse-entry1.sh` rewritten around six SELECTs captured either side of the apply — the one thing `--verify` cannot know, that nothing outside the retirement moved. No artefact files, no diff command — v5.13.1
- **Superseded files removed:** `readme/audit/TagCleanupSummary-2026-08.md`, `readme/audit/LitterObjectBacklog-2026-08.csv` and `readme/audit/TagMigrationQueue-2026-08.csv`. `readme/PostTagMigrationClean.md` §2 is the record. `LitterObjectInventory-2026-08.csv` and `TagPairMigrationManifest-2026-08.csv` stay — measured evidence, not superseded process — v5.13.1
- Removed `docs/GOODMORNING.md` from the branch — session handoff scratch, overwritten every session, not project documentation — v5.13.1
- Recorded the `createTagLegacy` resurrection-guard gap as a known limitation in the design doc rather than fixing it: the guard covers `createTagFromClo` only, and the durable fix is a `PhotoTag::saving` guard covering both paths, which is its own change — v5.13.1

### Rehearsal (iteration 1, `olm_postmig_3`)

- **The rehearsal immediately caught a bug in the newly folded-in reconciliation.** `Query\Builder::pluck($column, $key)` re-selects only the two columns it is handed, discarding the `selectRaw` aliases; on the grouped per-scope query it returned 0 where the real figure was 951, and `--verify` reported 1,631 of 1,646 scopes disagreeing. MySQL and Redis were in fact identical. Fixed by building the map from `get()` — v5.13.1
- Every existing test passed straight through that bug because no fixture photo carried a `country_id`, so only the global scope was ever exercised. Added `test_verify_reconciles_country_and_user_scopes`, confirmed to fail when the `pluck()` form is reintroduced — v5.13.1
- Post-fix run: all five checks pass, object 149 reconciled across **1,646 scopes**, object 92 across 1 (drained). Globals byte-identical either side — `photo_tags` 617,044 rows / 837,278 items and 536,956 live photos / 4,744,284 XP unchanged. Object 149 went 10,051 → 10,304 rows and 12,946 → 13,210 items (exactly the 253 rows / 264 items moved), pivot 111 → 176, all 128 quick tags repointed intact — v5.13.1
- `olm:redis:rebuild` reproduced the §8a litter gap exactly (834,188 MySQL vs 808,849 Redis) on a fresh import — constant across the retirement, still its own open ticket — v5.13.1

### Known state

- Prior rehearsal evidence in `TagRetirements-2026-08.csv` is **void** — `--verify` changed shape (10 checks → 5, global Redis → all-scope), so the recorded "Verify 10/10, byte-identical 3x" no longer describes this command. The cell is left untouched pending a completed re-rehearsal — v5.13.1
- Only **iteration 1 of 3** has run. The determinism bar (identical before/after/verify hashes across three restores) is NOT met — iterations 2 and 3 need a re-import each. `olm_postmig_3` now holds post-apply state — v5.13.1
- Entry 1 remains at `MAPPING_APPROVED` in the canonical list; the rehearsal drove a working copy. Step B (the `TagsConfig` key swap and translation surfaces) is still outstanding — v5.13.1
