# 2026-08-10

## Session — attribution fix and entry 1 to CODE_UPDATED (`bugs/2026/08/plastic-bag-count`)

- `olm:migrate-tag --advance` no longer destroys prior attribution. `approver`/`approved_at` are written once — they record the mapping approval — and every rung after that is appended to `verification_evidence` as `STATUS by <who> at <when>`. Previously each advance overwrote both columns, so the first transition past `MAPPING_APPROVED` erased the record of who approved the entry and when — v5.13.1
- `MigrateTagTest` — added `test_advancing_preserves_the_original_approval_and_records_every_transition` (two successive advances: original approver intact, both transitions and the rehearsal evidence all present) and a `queueRow()` helper to read the queue file back — v5.13.1
- `PostTagMigrationClean.md` §4 B — recorded the ordering caveat: the lifecycle puts `CODE_UPDATED` before the apply, so B.4–B.7 land before the data moves, inverting the prose order. Safe only where the config edit is inert until the retirement runs (verified for entry 1 — `TagsConfig` object keys feed only the suggested types/materials in `getAllTags()`, the picker reads the database), and must be re-checked per entry — v5.13.1
- `PostTagMigrationClean.md` §4 E — new production-run checklist: confirm the database before any dry run (a rehearsal leaves `.env` on a post-apply database where `expectationsMatch` refuses), confirm production runs this branch before trusting `--verify` (the reconciliation was wrong before `99502335`), run the entry in one sitting, expect the 2026-08-08 counts to have drifted since the picker stayed open, and B.3 `crowdsourced` is accepted-cosmetic — v5.13.1
- **Entry 1 `other--plastic_bag` advanced `MAPPING_APPROVED` → `CODE_UPDATED`** on the canonical list, by sean. Original `product-owner` / `2026-08-08` approval intact; rehearsal evidence intact; transition appended. `DRY_RUN_VERIFIED` deliberately not taken — it is perishable and belongs immediately before the production apply — v5.13.1
