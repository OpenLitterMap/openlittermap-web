# 2026-08-14

## Session — tag retirement: plastic_bag → plasticBags (v5.14.0)

- `plasticBags` replaces `plastic_bag` in `TagsConfig` and all locales — v5.14.0
- Litter objects can be retired and merged into a survivor — v5.14.0
- Retired objects are refused on tag write and quick-tag sync, and hidden from the picker — v5.14.0
- New `olm:migrate-tag` retires one approved object into another, with verify and Redis repair — v5.14.0
- Metrics skip user-scoped writes for photos with no owner — v5.14.0
- `olm:fix-orphaned-tags` now refuses to run — v5.14.0
- Changelog rule tightened: shortest summary, critical information only — v5.14.0

**Migration required** — adds `retired_at` and `merged_into_id` to `litter_objects`.

**No production data has moved.** Retiring entry 1 needs a fresh rehearsal and a full tag-write
freeze (`php artisan down` on every serving node). Runbook: `readme/PostTagMigrationClean.md` §E.
