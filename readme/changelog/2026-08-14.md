# 2026-08-14

## Session — tag retirement: plastic_bag → plasticBags (v5.14.0)

Branch `bugs/2026/08/plastic-bag-count`. Retires duplicate litter object `plastic_bag` (92) into
`plasticBags` (149) — decision D-4, product-owner approved. Design and runbook:
`readme/PostTagMigrationClean.md`.

- `TagsConfig` and 9 translation files now use `plasticBags`; the duplicate `plastic_bags` key is gone from both English files — v5.14.0
- `litter_objects` gains `retired_at` and `merged_into_id`; `LitterObject::active()` and `isRetired()` — v5.14.0
- Retired objects are refused on write and hidden on read: `AddTagsToPhotoAction` (CLO and legacy paths), `SyncQuickTagsAction`, `AutoCreateBrandRelationships`, `GET /api/tags`, `GET /api/tags/all` — v5.14.0
- New `olm:migrate-tag` — one approved entry at a time from `readme/audit/TagRetirements-2026-08.csv`: lifecycle gating, preflight-first apply, resumable snapshot, quick-tag repointing, `--verify` across MySQL and Redis at every scope, `--repair-redis` — v5.14.0
- Redis recovery is four steps: `--apply` (fails) → `--repair-redis` → `--apply` → `--verify`; only a successful apply clears the snapshot — v5.14.0
- Production runs require `php artisan down` on every serving node — the `retired_at` guards do not cover a tag replace that omits the object — v5.14.0
- `MetricsService` and `RedisMetricsCollector` skip user-scoped writes when `photos.user_id` is null — v5.14.0
- `olm:fix-orphaned-tags` refuses to run; its mappings encode the pre-D-4 direction — v5.14.0
- Entry 1 is at `CODE_UPDATED`, blocked on a fresh production-snapshot rehearsal; the 2026-08-09 evidence is void — v5.14.0
- Changelog rule in `CLAUDE.md` tightened to core logic / result only, one file per deliverable — v5.14.0

Tests: `MigrateTagTest` 55, `QuickTagsApiTest` + `RedisMetricsCollectorNullUserTest` 41.
